{
  "name": "@icp-cc/ic-agent-web-probe",
  "version": "0.0.0",
  "private": true,
  "description": "Headless-Chromium harness that verifies the R-3b WU-0 agent-js PoC (one real anonymous canister query through the backend CORS proxy). Self-contained so the root deps-allowlist is untouched.",
  "engines": { "node": ">=22" },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
