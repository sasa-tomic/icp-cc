{
	"name": "cloudflare-api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --config wrangler.prod.jsonc",
		"dev": "rm -rf .wrangler/state/v3/d1/miniflare-D1DatabaseObject/* && wrangler dev --config wrangler.local.jsonc",
		"start": "rm -rf .wrangler/state/v3/d1/miniflare-D1DatabaseObject/* && wrangler dev --config wrangler.local.jsonc",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.45.0"
	},
	"dependencies": {
		"luaparse": "^0.3.1"
	}
}
