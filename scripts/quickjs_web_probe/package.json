{
  "name": "@icp-cc/quickjs-web-probe",
  "version": "0.0.0",
  "private": true,
  "description": "Headless-Chromium harness that verifies the R-3 WU-1 QuickJS-on-Web probe (built via `flutter build web`). Self-contained so the root deps-allowlist is untouched.",
  "engines": { "node": ">=22" },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
